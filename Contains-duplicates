class Solution(object):
    def containsDuplicate(self, nums):
        #for i in range(len(nums)):
           # if nums[i]<0:
                #nums[i]=nums[i]*(-1)

        
        s=set(nums)
        s=list(s)
        nums.sort()
        s.sort()
        if s==nums:
            return False
        else:
            return True
