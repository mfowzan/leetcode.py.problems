nums=[]
target=int(input("Enter your target number"))
while True:
    b=input("enter the number you want to add and leave blank to stop")
    if b=='':
        break
    else:
       nums=nums+[int(b)]

sol=[]
index=0
for i in nums:
  if nums[ind]+ i == target:
     sol=sol+[ind]
     sol=sol + [nums.index(i)]
     print(nums)
     print(sol)
  elif i==nums[-1]:
     index=index+1
